import assert from 'node:assert/strict';
import { alternativeProteinCapability, ensureAlternativeProteins, tickAlternativeProteins } from '../js/economy/alternativeProteins.js?v=20260922-alt-protein1';
import { electricityDemand } from '../js/economy/electricity.js?v=20260922-alt-protein1';
import { regionalWaterDemand } from '../js/world/waterResources.js?v=20260922-alt-protein1';
import { ensureLivestockAgriculture, tickLivestockAgriculture } from '../js/economy/livestockAgriculture.js?v=20260922-alt-protein1';

function modern(){return {id:'modern',population:500000,unlockedTechIds:new Set(['industrial_electrification','refrigeration','germ_theory','vaccination']),electricity:{industrialService:.95,generated:100},publicEducation:{literacy:.92},industrialSupply:{capability:{precision_machining:.9,steelmaking:.6}},cropBiotechnology:{platformMaturity:.85,capability:.9},medicalProgress:{researchCapability:.9},computingIndustry:{capacity:{chip_design:14,wafer_fab:14},experience:{chip_design:.85,wafer_fab:.8}},stockpile:{computers:1000,pulses:1000,staple_grains:1000,animal_foods:0},foodDiversity:{shortage:{animal_foods:.35},productionMix:{animal_foods:.24}},waterResources:{demand:{industry:1},allocation:{industry:1}},terrain:{hills:.3,plains:.4,forest:.1},landQuality:1.1,governance:{administrativeControl:.8},agriculturalMachinery:{tractorCoverage:.8,combineCoverage:.7},precisionAgriculture:{adoption:.7},cropBreeding:{capability:.85},report:{}};}
function old(){return {id:'old',population:500000,unlockedTechIds:new Set(),electricity:{industrialService:0},publicEducation:{literacy:.15},industrialSupply:{capability:{precision_machining:0}},cropBiotechnology:{platformMaturity:0,capability:0},medicalProgress:{researchCapability:0},stockpile:{pulses:1000,staple_grains:1000,animal_foods:0},foodDiversity:{shortage:{animal_foods:.5}},report:{}};}

{const r=old();assert.equal(alternativeProteinCapability(r),0);for(let y=0;y<40;y++)tickAlternativeProteins(r,365.2425);assert.equal(r.alternativeProteins.totalOutput,0,'premodern regions should not make industrial alternative protein');}
{const r=modern(),s=ensureAlternativeProteins(r);s.investment=1;const feedBefore=r.stockpile.pulses+r.stockpile.staple_grains;for(let y=0;y<35;y++)tickAlternativeProteins(r,365.2425);assert.ok(s.precisionFermentationMaturity>.3,'precision fermentation should mature in a capable economy');assert.ok(s.cultivatedMeatMaturity>.2,'cultivated meat should mature more slowly but become viable');assert.ok(s.totalOutput>0,'alternative protein plants should make animal-food-equivalent output');assert.ok(r.stockpile.animal_foods>0,'output should enter the animal-food stockpile');assert.ok(r.stockpile.pulses+r.stockpile.staple_grains<feedBefore,'production should consume carbohydrate feedstock');assert.ok(s.electricityLoad>0&&s.waterDemand>0,'production should require electricity and water');assert.ok(s.unitCostIndex<4,'learning and scale should lower cost');assert.ok(s.livestockDisplacement>0,'successful alternative proteins should substitute for conventional livestock');}
{const r=modern(),s=ensureAlternativeProteins(r);s.electricityLoad=.44;s.waterDemand=.12;const e=electricityDemand(r,7),w=regionalWaterDemand(r);assert.equal(e.alternativeProteinDemand,.44,'alternative protein electricity must enter grid demand');assert.ok(w.alternativeProteins>=.12,'alternative protein water should enter industrial water demand');}
{const baseline=modern(),substituted=modern();ensureAlternativeProteins(substituted).livestockDisplacement=.6;const a=ensureLivestockAgriculture(baseline),b=ensureLivestockAgriculture(substituted);a.intensiveShare=.55;b.intensiveShare=.55;baseline.stockpile.staple_grains=1000;substituted.stockpile.staple_grains=1000;tickLivestockAgriculture(baseline,30);tickLivestockAgriculture(substituted,30);assert.ok(b.marketScale<a.marketScale,'alternative protein substitution should contract conventional livestock scale');assert.ok(b.feedDemand<a.feedDemand,'substitution should reduce livestock feed demand');assert.ok(b.manureNutrientLoad<a.manureNutrientLoad,'substitution should reduce manure load');assert.ok(b.methaneEmissions<a.methaneEmissions,'substitution should reduce livestock methane');}
console.log('alternative proteins regression passed');

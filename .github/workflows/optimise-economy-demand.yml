name: Optimise economy demand aggregation
on:
  push:
    paths:
      - '.github/workflows/optimise-economy-demand.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Patch and test
        run: |
          python3 tools/optimise-economy-demand.py
          node --check js/economy/laborCore.js
          node -e "import('./js/economy/laborCore.js')"
          git diff --check
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add js/economy/laborCore.js
          git commit -m "Aggregate workshop demand in one neighbour pass"
          git push
